# Required. Generate a long random value and store it in a password manager.
OOMOL_CONNECT_ADMIN_TOKEN=replace-with-a-long-random-admin-token

# Required. Generate a different long random value. Losing it makes encrypted D1 data unreadable.
OOMOL_CONNECT_ENCRYPTION_KEY=replace-with-a-different-long-random-encryption-key
